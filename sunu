<?php

require_once __DIR__ . '/vendor/autoload.php';

use App\Core\Application;
use App\Core\Console\Kernel;

$app = new Application(__DIR__);
$app->boot();

// DB is connected in boot()

$kernel = new Kernel($app);
$kernel->handle($argv);
